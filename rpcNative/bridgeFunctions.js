import { Sum, PrintLog, PrintLocation, Camera } from './utilities';

const bridgeFunctions = {
    'Sum': Sum,
    'PrintLog': PrintLog,
    'PrintLocation': PrintLocation,
    'Camera': Camera,
};

export default bridgeFunctions;