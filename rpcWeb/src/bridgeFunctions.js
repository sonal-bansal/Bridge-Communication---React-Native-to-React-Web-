import { Sum, PrintLog, ShowAlert, StoreImageInWeb, getLocation, LocationPermissionAlert } from './utilites';

const bridgeFunctions = {
    'Sum': Sum,
    'PrintLog': PrintLog,
    'ShowAlert': ShowAlert,
    'StoreImageInWeb': StoreImageInWeb,
    'getLocation': getLocation,
    'LocationPermissionAlert': LocationPermissionAlert
};

export default bridgeFunctions;